const go_btn = document.getElementById("go-button");
const sticker_pkg =document.getElementById("sticker-pkg");


go_btn.addEventListener('click', open_pkg);

function open_pkg() {
    let random = Math.random()

    if (random <= 0.1){

    }
    
    else if (random <= 0.3){

    }

    else{

    }
    
    window.open(indexedDB.html);
}